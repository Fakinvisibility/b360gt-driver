# 安全政策

## 支持版本

项目仍处于发布候选阶段。安全修复只会应用到 `main` 和最新发布候选版本；
较早的提交、分支和本地构建包不提供安全维护承诺。

| 版本 | 安全更新 |
| --- | --- |
| `main` | 支持 |
| 最新 `1.0.0rc*` | 支持 |
| 更早版本 | 不支持 |

## 私密报告漏洞

请不要通过公开 Issue 报告尚未修复的安全漏洞。请使用仓库 `Security` 页面中的
`Report a vulnerability` 私密报告入口。

报告中尽量包含：

- 受影响的版本或 commit；
- 操作系统、Python 版本和安装方式；
- 可重复的最小步骤；
- 预期影响和实际影响；
- 不包含个人信息或无关设备流量的日志；
- 如果涉及 USB，说明 VID/PID、接口和端点，但不要提供设备序列号。

维护者通常会在 7 天内确认收到报告。确认问题后，会在公开修复前通过私密安全
通告协调验证和披露时间。

## 安全范围

以下问题应当私密报告：

- 绕过 VID/PID、接口、端点或 HID 报告白名单；
- 通过媒体文件造成任意代码执行、路径逃逸或非预期文件删除；
- 绕过本机 Web API 的 Host/Origin 限制；
- 从后台服务获得超出当前用户权限的能力；
- 泄露媒体库内容、设备信息或本机隐私数据。

一般使用问题、设备不兼容、画面方向、播放性能和功能建议可以使用公开 Issue。

## 敏感资料

请勿提交或附加：

- 厂商固件、安装包、密钥或其他专有文件；
- 原始整机 USB 抓包；
- 设备序列号；
- 包含其他 USB 设备流量的日志；
- GitHub Token、私钥、账户信息或真实用户路径。

如需提供用于复现的恶意媒体样本，请先在私密报告中联系维护者，不要上传到
公开 Issue 或 Pull Request。
